// Intentionally left blank.
//
// Note: SwiftData requires newer SDK/toolchain than is currently available in this repo’s build environment.
// We use a lightweight SQLite store instead (see SQLiteStore.swift).
